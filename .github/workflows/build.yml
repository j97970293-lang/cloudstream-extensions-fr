name: Build and Publish

on:
  push:
    branches: [ master ]
    paths-ignore:
      - '**.md'
      - 'docs/**'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout source
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Build plugins and generate manifest
        env:
          GITHUB_REPOSITORY: ${{ github.repository }}
        run: |
          chmod +x ./gradlew
          ./gradlew --no-daemon make makePluginsJson

      - name: Validate Nuvio providers
        run: |
          find nuvio -type f -name '*.js' -print0 | xargs -0 -n1 node --check

      - name: Prepare published files
        run: |
          rm -rf published
          mkdir -p published
          test -f build/plugins.json
          cp build/plugins.json published/plugins.json
          cp repo.json published/repo.json
          test -f docs/frenchhub.png
          cp docs/frenchhub.png published/frenchhub.png
          find . -path '*/build/*.cs3' -type f -exec cp {} published/ \;
          test "$(find published -maxdepth 1 -name '*.cs3' | wc -l)" -gt 0
          find published -maxdepth 1 -type f -printf '%f\n' | sort

      - name: Prepare CDN mirror files
        run: |
          rm -rf published-mirror
          cp -R published published-mirror
          sed -i 's#https://raw.githubusercontent.com/j97970293-lang/cloudstream-extensions-fr/builds/#https://j97970293-lang.github.io/cloudstream-extensions-fr/#g' published-mirror/plugins.json
          sed -i 's#https://raw.githubusercontent.com/j97970293-lang/cloudstream-extensions-fr/master/docs/frenchhub.png#https://j97970293-lang.github.io/cloudstream-extensions-fr/frenchhub.png#g' published-mirror/plugins.json
          sed -i 's#https://raw.githubusercontent.com/j97970293-lang/cloudstream-extensions-fr/builds/plugins.json#https://j97970293-lang.github.io/cloudstream-extensions-fr/plugins.json#g' published-mirror/repo.json

      - name: Publish builds branch
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./published
          publish_branch: builds
          force_orphan: true
          commit_message: "Build ${{ github.sha }}"

      - name: Publish CDN mirror branch
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./published-mirror
          publish_branch: mirror
          force_orphan: true
          commit_message: "Mirror ${{ github.sha }}"
